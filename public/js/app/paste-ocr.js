import Tesseract from "tesseract.js"

(() => {
    const worker = Tesseract.createWorker('chi_tra')
    console.log('load')
})()

/**
 *
 * @param {HTMLElement} el - The alpinejs $el
 * @returns {Promise<void>}
 */
const pasteNamePic = async (el) => {

}
